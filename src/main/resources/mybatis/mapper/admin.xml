<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-/mybatis.org/DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.human.springboot.AdminController.AdminDAO">
	<select id="prolist" resultType="com.human.springboot.AdminController.AdminDTO">
	select book_num, book_name, AUTHOR, book_price, book_genre, EMOTION, RATING
	from Book
	ORDER BY book_genre
	</select>
	<insert id="newProduct">
		insert into Book (book_num, book_name, book_price, book_genre, book_content,
		author,publication,rating,emotion,book_summary,book_cover, book_detail)
		values (book_num.nextval, #{param1}, #{param2}, #{param3},#{param4},#{param5},
		#{param6},#{param7},#{param8},#{param9}, '0','0')
	</insert>
	<update id="newUrl">
		update Book set book_cover=#{param1} where book_name=#{param2}
	</update>
	<update id="newUrlC">
		update Book set book_detail=#{param1} where book_name=#{param2}
	</update>
</mapper>
